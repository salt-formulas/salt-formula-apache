applications:
- apache
parameters:
  apache:
    server:
      enabled: true
      bind:
        address: 0.0.0.0
        ports:
        - 80
        - 443
        protocol: tcp
      modules:
      - ssl
      - rewrite
